# Server
PORT=3000
NODE_ENV=development

# CORS (comma-separated list or a single origin)
CORS_ORIGIN=http://localhost:5173

# Auth (change in real .env)
JWT_SECRET=replace_me_with_a_long_random_string

# Database (Prisma/Postgres example)
DATABASE_URL=postgresql://user:password@localhost:5432/thrivev2?schema=public
